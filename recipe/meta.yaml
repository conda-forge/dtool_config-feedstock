{% set name = "dtool_config" %}
{% set reponame = "dtool-config" %}
{% set author = "jic-dtool" %}
{% set version = "0.4.0" %}
{% set sha256 = "79a08c55c266e38f7f7ff976fb2aa6a0d63945c713ecefa1155c0a6abd30a6f0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/{{ author }}/{{ reponame }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  preserve_egg_dir: True
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python
    - click
    - setuptools
    - dtoolcore
    - dtool_cli

test:
  imports:
    - dtool_config
    - dtool_config.cli
    - dtool_config.utils
  commands:
    - dtool config --help

about:
  home: http://github.com/{{ author }}/{{ reponame }}
  license: MIT
  license_file: LICENSE.rst
  summary: 'dtool CLI commands for configuring dtool settings'

  dev_url: https://github.com/{{ author }}/{{ reponame }}

extra:
  recipe-maintainers:
    - pvanheus
